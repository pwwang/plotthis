% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{force_promises}
\alias{force_promises}
\title{Force all bindings in an environment}
\usage{
force_promises(env = parent.frame())
}
\arguments{
\item{env}{The environment whose bindings to force.
Default: the caller's frame.}
}
\value{
Invisibly \code{NULL}.
}
\description{
Evaluates every binding in \code{env} so that unforced argument promises
(created by lazy evaluation) are replaced by their values. This prevents
\code{env} from keeping its calling frame alive when it is referenced by a
returned object (e.g. a ggplot's \code{plot_env}) and later serialized
(e.g. by \code{saveRDS()}), which would otherwise pull the caller's data into the
serialized object.
}
\keyword{internal}
